# Fixed Requirements for Email Automation MCP Server
# Core MCP and email automation dependencies

# MCP Server Framework (matching your working form server version)
fastmcp>=0.1.0
pydantic>=2.0.0

# Email functionality (built-in Python modules, no extra packages needed)
# - smtplib (built-in)
# - email (built-in)
# - mimetypes (built-in)

# Environment and configuration
python-dotenv>=1.0.0

# Optional: Enhanced email features
# For OAuth2 email authentication (Gmail, Outlook)
# google-auth>=2.0.0
# google-auth-oauthlib>=0.5.0

# For email template support (if needed)
# jinja2>=3.0.0

# Development/testing (optional)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0

# Railway deployment (if using Railway)
# gunicorn>=21.0.0
# uvicorn>=0.23.0

# System dependencies notes:
# - No additional system dependencies required for basic email functionality
# - SMTP access to your email provider required
# - For Gmail: Enable 2FA and generate app password
# - For corporate email: Check with IT for SMTP settings